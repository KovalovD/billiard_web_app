# Add these to Railway's environment variables
# DO NOT commit this file to Git!

APP_NAME="WinnerBreak"
APP_ENV=production
APP_KEY=base64:BR+ZzpEQ2xp6waBf1YzF95WP7r5GDv14FHErtc4o8JE=
APP_DEBUG=false
APP_URL=https://YOUR-APP-NAME.up.railway.app

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Railway will auto-inject these MySQL variables:
# DB_CONNECTION=mysql
# MYSQL_HOST
# MYSQL_PORT
# MYSQL_DATABASE
# MYSQL_USER
# MYSQL_PASSWORD
# MYSQL_URL

# Set these manually in Railway:
DB_CONNECTION=mysql
DB_HOST=${MYSQL_HOST}
DB_PORT=${MYSQL_PORT}
DB_DATABASE=${MYSQL_DATABASE}
DB_USERNAME=${MYSQL_USER}
DB_PASSWORD=${MYSQL_PASSWORD}

BROADCAST_DRIVER=log
CACHE_DRIVER=database
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Mail configuration (optional - add your SMTP details)
MAIL_MAILER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@b2bleague.com"
MAIL_FROM_NAME="${APP_NAME}"

# CORS configuration
SANCTUM_STATEFUL_DOMAINS=${APP_URL}
SESSION_DOMAIN=.railway.app

# Telegram Bot (if needed)
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBAPP_DATA_VALIDATION_ENABLED=false

# Vite
VITE_APP_NAME="${APP_NAME}"
